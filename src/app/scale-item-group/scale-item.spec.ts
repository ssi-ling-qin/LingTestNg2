/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {ScaleItem} from './scale-item';

describe('ScaleItem', () => {
  it('should create an instance', () => {
    expect(new ScaleItem()).toBeTruthy();
  });
});
