/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {AnswerItem} from './answer-item';

describe('AnswerItem', () => {
  it('should create an instance', () => {
    expect(new AnswerItem()).toBeTruthy();
  });
});
